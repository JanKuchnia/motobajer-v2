---
import { Phone, Mail, Wrench, CalendarDays, Star, MapPin } from 'lucide-astro';
const year = new Date().getFullYear();

const links = [
  { href: '#uslugi', label: 'Usługi', icon: Wrench },
  { href: '#rezerwacja', label: 'Rezerwacja', icon: CalendarDays },
  { href: '#opinie', label: 'Opinie', icon: Star },
  { href: '#lokalizacje', label: 'Lokalizacje', icon: MapPin },
  { href: '#kontakt', label: 'Kontakt', icon: Phone },
];
---

<footer class="bg-navy-dark text-ink-on-navy">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 py-14">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-10 mb-10">
      <!-- Brand -->
      <div>
        <p
          class="text-xl font-bold text-white mb-3"
          style="font-family: var(--font-display); letter-spacing: 0.05em;"
        >
          MOTOBAJER
        </p>
        <p class="text-sm leading-relaxed" style="color: oklch(68% 0.04 265);">
          Stanisław Bajer<br />
          Autoryzowany serwis sieci EuroWarsztat<br />
          Myślenice, od 2008 roku
        </p>
      </div>

      <!-- Quick links -->
      <div>
        <h3 class="text-xs font-semibold uppercase tracking-widest mb-4" style="color: oklch(55% 0.06 265);">
          Nawigacja
        </h3>
        <ul class="space-y-2">
          {links.map((link) => (
            <li>
              <a
                href={link.href}
                class="inline-flex items-center gap-1.5 text-sm hover:text-white transition-colors"
                style="color: oklch(68% 0.04 265);"
              >
                <link.icon class="w-3.5 h-3.5 shrink-0" aria-hidden="true" />
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Contact -->
      <div>
        <h3 class="text-xs font-semibold uppercase tracking-widest mb-4" style="color: oklch(55% 0.06 265);">
          Kontakt
        </h3>
        <ul class="space-y-2 text-sm" style="color: oklch(68% 0.04 265);">
          <li>
            <a href="tel:+48609845865" class="inline-flex items-center gap-1.5 hover:text-white transition-colors">
              <Phone class="w-3.5 h-3.5 shrink-0" aria-hidden="true" />
              609 845 865
            </a>
          </li>
          <li>
            <a href="tel:+48122742306" class="inline-flex items-center gap-1.5 hover:text-white transition-colors">
              <Phone class="w-3.5 h-3.5 shrink-0" aria-hidden="true" />
              (12) 274 23 06
            </a>
          </li>
          <li>
            <a href="mailto:motobajer@eurowarsztat.pl" class="inline-flex items-center gap-1.5 hover:text-white transition-colors">
              <Mail class="w-3.5 h-3.5 shrink-0" aria-hidden="true" />
              motobajer@eurowarsztat.pl
            </a>
          </li>
        </ul>
      </div>
    </div>

    <!-- Bottom bar -->
    <div
      class="pt-6 border-t flex flex-col sm:flex-row justify-between items-start sm:items-center gap-3 text-xs"
      style="border-color: oklch(100% 0 0 / 0.08); color: oklch(45% 0.04 265);"
    >
      <p>© {year} MOTOBAJER Stanisław Bajer. Wszelkie prawa zastrzeżone.</p>
      <p>NIP: 6811341146 · REGON: 120696804</p>
    </div>
  </div>
</footer>
